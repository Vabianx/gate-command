import{TEAM}from"./characters.js?v=0.5.1";import{WORLDS}from"./worlds.js?v=0.5.1";
export function freshGame(){return{version:"0.5.1",energy:100,research:0,personnel:4,completed:0,archive:[],baseStatus:"Keine Expedition aktiv.",worldFlags:{signalDetected:false,guardiansAwakened:false,precursorLogFound:false,watchersObserved:false,taekhanRecognized:false},unlockedWorldIds:["p4x761"],team:JSON.parse(JSON.stringify(TEAM)),worlds:JSON.parse(JSON.stringify(WORLDS))}}
export function normalizeGame(saved){const base=freshGame(),merged={...base,...saved};merged.worldFlags={...base.worldFlags,...(saved?.worldFlags||{})};if(saved?.worldFlags?.hiddenTeamZero)merged.worldFlags.precursorLogFound=true;merged.version="0.5.1";merged.team=Array.isArray(saved?.team)&&saved.team.length?saved.team:base.team;merged.worlds=base.worlds;merged.unlockedWorldIds=Array.isArray(saved?.unlockedWorldIds)&&saved.unlockedWorldIds.length?saved.unlockedWorldIds:base.unlockedWorldIds;merged.archive=Array.isArray(saved?.archive)?saved.archive:[];merged.personnel=merged.team.filter(x=>x.health!=="vermisst"&&x.health!=="gefallen").length;return merged}
export function addWorldFlag(game,key,value=true){game.worldFlags[key]=value}
export function unlockWorld(game,id){if(!game.unlockedWorldIds.includes(id))game.unlockedWorldIds.push(id)}